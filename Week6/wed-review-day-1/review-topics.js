//JS state Machines

    // light switch

    // off to on 

    // on to off

    // only one state at a time

    // see mon for updated notes on the traffic light example

// OOP Design

 // see replay for breakdown from class

// Promises and Async

  //updated the demo with a bad example see past demo

// JS Functions
// how to write one from scratch 
    // use usersnippets to your advantage
    // fun ninjas play chess
    // function name parenthesis curly braces

    function name(parameters){

    }

    // type in function
    // find the semi completed box and hit tab
    // use it as a cheatsheet

// arrow functions
   // create a custom user snippet for this
   //play fun chess
   //()=>{}
   // array.find(<arrowFunctionHere>)
   // let someFunction = <arrowFunctionHere>

// naming
   // methods are actions
   // be as descriptive as possible without being redundant
   // don't include implementation details in function name

   // camelCase - first letter lower - following words first letter caps
    // getDataInformationStuff() // bad
    // getData() //ok
    // getCustomerInfo() // better
    // getcustomerinfo() // not using camel case and less readable

// JS Loops
let array = [1,2,3]
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    console.log(element);
}

for (const item of array) {
    console.log(array)
}

// JS IO
 // see demo examples with updated comments

// template literals
  // wysiwyg
  const animal = "dog";
  const person = "Doug";
  const year = 1942;

  // string concat
  const mySentence = "Once I was walking my " + animal + " and I saw " + person + "\n driving a car from " + year;
  const mytemplateSentence = `Once I was walking my ${animal} and I saw ${person}
   driving a car from ${year}`;

// JS Boolean & Logic Gates
    // true or false

    // if statements and have branching logic

// Classes

  // keep it classy

// Object Methods

let myObjectivelyAwesomeReallyCoolObject = {
    a: 1,
    b: 22,
    c: 333
};
myObjectivelyAwesomeReallyCoolObject
console.log(Object.keys(myObjectivelyAwesomeReallyCoolObject)) // returns ['a','b','c']
console.log(Object.entries(myObjectivelyAwesomeReallyCoolObject))

// JS Arrays in Depth





